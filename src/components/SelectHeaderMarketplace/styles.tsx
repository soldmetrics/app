import styled from 'styled-components/native';
import DropDownPicker from 'react-native-dropdown-picker';
import { Image } from 'expo-image';

export const Wrapper = styled(DropDownPicker)`

`;

export const Icon = styled(Image)`
  width: 24px;
  height: 24px;
`;
